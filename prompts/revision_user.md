Here is independent review feedback. Revise the memo and workbook accordingly. Do not restart from scratch unless the review explicitly requires it.

The prior workbook is attached. Use **code execution** to open it and apply revisions. **Preserve formulas** — only change input cells or fix specific issues noted in the review.

Save the updated workbook as `workbook_v{version}.xlsx` via code execution.

Output the revised memo between `--- MEMO START ---` and `--- MEMO END ---`.

After saving, output key results between:
--- WORKBOOK RESULTS START ---
[BCR, NPV, key metrics]
--- WORKBOOK RESULTS END ---

If using guide_workbook.xlsm tabs, use ONLY these exact names:
{valid_tabs}

--- REVIEW FEEDBACK ---
{review_text}
